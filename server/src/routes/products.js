// A modo de ejemplo, vamos a crear un archivo products.js en la carpeta routes con el siguiente contenido:

// import express from 'express';
// import ProductsController from '../controllers/Products.js';

// const router = express.Router();

// // Endpoint para obtener la lista de productos
// router.get('/products', ProductsController.getProducts);

// // Endpoint para validar un producto
// router.get('/product/validate/:id', ProductsController.validateProduct);

// export default router;
